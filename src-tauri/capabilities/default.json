{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core IPC only. Process execution is performed by native Rust commands (tokio::process), which are not exposed to the webview, so no shell-plugin permissions are granted.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
